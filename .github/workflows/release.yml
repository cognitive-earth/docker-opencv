# .github/workflows/release.yml
name: Reuse Node Release Workflow

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  node-release:
    uses: cognitive-earth/github-actions-workflows/.github/workflows/node-release.yml@master
    secrets: inherit
